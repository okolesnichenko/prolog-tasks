domains
  col = symbol
  name = symbol
predicates
  girl(name)
  color(col)
  pair(name, col)
clauses
  girl(Anya).
  girl(Nina).
  girl(Natasha).
  color(white).
  color(blue).
  color(green).
  pair(X, Y):- girl(X), color(Y), X = Natasha, Y = green.
  pair(X, Y):- girl(X), color(Y), X = Nina, not(Y = white), not(Y = green).
  pair(X, Y):- girl(X), color(Y), X = Anya, not(Y=green), not(Y = blue).
%goal
%pair(X,Y).
